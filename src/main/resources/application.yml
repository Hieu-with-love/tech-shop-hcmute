# declare global variable
SERVER_PORT: 8080

#port of smtp protocol
EMAIL_HOST: smtp.gmail.com
EMAIL_PORT: 587
EMAIL_ID: hieu01bdvn@gmail.com
EMAIL_PASSWORD: ocvqndmbczmueyks
VERIFY_EMAIL_HOST: http://localhost:${SERVER_PORT}

# server run with port 8080 - default
server:
  port: ${SERVER_PORT}

#config database connection with mysql
spring:
  web:
    resources:
      static-locations: file:uploads/, classpath:/static/
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/tech_shop?useSSL=false&serverTimezone=UTC
    username: root
    password: Tuantp2004@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  mail:
    host: ${EMAIL_HOST}
    port: ${EMAIL_PORT}
    username: ${EMAIL_ID}
    password: ${EMAIL_PASSWORD}
    default-encoding: UTF-8
    properties:
      mail:
        mime:
          charset: UTF
        smtp:
          writetimeout: 10000
          connectiontimeout: 10000
          timeout: 10000
          auth: true
          starttls:
            enable: true
            required: true
    verify:
      host: ${VERIFY_EMAIL_HOST}

jwt:
  signedKey: "zsB+WqpHBR2Lorx/E7adcBsor1NwJQswYtB/bnFZdfThjCXxwyDGlpRtI1dcTPHE"
